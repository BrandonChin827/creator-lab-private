# Creator Lab assets

No user-provided founder photos or client assets were available in this pass.

The landing page therefore uses only original CSS/SVG graphics and clearly labeled sample interface artifacts. Do not add a generated or substitute founder portrait, or present illustrative artifacts as client proof.

- `photos/` — reserved for approved, user-provided photography.
- `artifacts/` — swap point for approved, honestly labeled work samples. Replace the three inline sample mini-interfaces only when real artifacts are supplied and permission to publish is confirmed.
- `graphics/` — swap point for original supporting graphics; the current Creator OS mechanism and ambient grid remain inline CSS so the page stays single-file.

The three outcome samples and three-part artifact rail in `index.html` are illustrative interface content, not client work or performance evidence.
